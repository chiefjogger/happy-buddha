{
  "name": "wish-prayer-website",
  "version": "1.0.0",
  "description": "A website for sending wishes and prayers",
  "main": "script.js",
  "scripts": {
    "build": "mkdir -p public && echo \"window.ENV = { OPENAI_API_KEY: '$OPENAI_API_KEY' }\" > public/config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "wish",
    "prayer",
    "buddha"
  ],
  "author": "",
  "license": "ISC"
}
